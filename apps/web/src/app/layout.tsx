import type { Metadata } from "next";
import { Inter, Outfit } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
  display: "swap",
});

const outfit = Outfit({
  variable: "--font-outfit",
  subsets: ["latin"],
  display: "swap",
});

export const metadata: Metadata = {
  title: "FleetForce — Industrial Fleet Management System",
  description:
    "Enterprise-grade fleet management platform for construction & heavy equipment. Real-time GPS tracking, driver management, maintenance scheduling, and AI-powered insights.",
  keywords: [
    "fleet management",
    "GPS tracking",
    "construction equipment",
    "vehicle management",
    "driver management",
  ],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={`${inter.variable} ${outfit.variable}`} suppressHydrationWarning>
      <body className="min-h-screen bg-background font-sans antialiased">
        {children}
      </body>
    </html>
  );
}
